/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package real_hunch.app;

import real_hunch.list.LinkedList;

import static real_hunch.utilities.StringUtils.join;
import static real_hunch.utilities.StringUtils.split;
import static real_hunch.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
